# Advanced ML Analyzer: Комплексный анализ классификации и кластеризации

Advanced ML Analyzer - это мощный инструмент на Python для комплексного анализа данных, включающий алгоритмы классификации и кластеризации с продвинутой визуализацией.

## 📋 Оглавление
+ Возможности

+ Быстрый старт

+ Детальное использование

+ Архитектура

+ Примеры использования

+ Визуализации

+ Производительность


## 🚀 Возможности
### Классификация
+ ✅ Logistic Regression

+ ✅ Decision Tree

+ ✅ Random Forest

+ ✅ Support Vector Machine (SVM)

+ ✅ K-Nearest Neighbors (KNN)

+ ⚡ Автоматическая оптимизация гиперпараметров

+ 📊 Кросс-валидация (5-fold)

+ 📈 Подробные метрики оценки

### Кластеризация
+ ✅ K-Means

+ ✅ DBSCAN

+ ✅ Agglomerative Clustering

+ ✅ Gaussian Mixture Models

+ 🔍 Автоматическое определение оптимального числа кластеров

+ 📐 Метрики: Silhouette, Davies-Bouldin, Calinski-Harabasz

### Визуализация
+ 📊 Разведочный анализ данных (EDA)

+ 📈 Сравнение моделей

+ 🎯 Confusion Matrix

+ 📉 ROC-кривые

+ 🔥 Важность признаков

+ 📐 PCA и t-SNE проекции

+ 🌳 Дендрограммы

+ 🎨 3D визуализация кластеров

### Обработка данных
+ 🧹 Автоматическая обработка пропущенных значений

+ ⚖️ Масштабирование признаков

+ 🔤 Кодирование категориальных переменных

+ 📋 Балансировка классов

+ 🎯 Разделение на train/test

## ⚡ Быстрый старт
### Пример 1: Быстрый анализ с встроенными данными
```python
from advanced_ml_analyzer import AdvancedMLAnalyzer

# Инициализация анализатора
analyzer = AdvancedMLAnalyzer(random_state=42)

# Загрузка встроенного датасета (Iris)
from sklearn.datasets import load_iris
import pandas as pd

data = load_iris()
df = pd.DataFrame(data.data, columns=data.feature_names)
df['target'] = data.target

# Основной анализ
analyzer.load_and_prepare_data(data=df, target_col='target')
analyzer.exploratory_visualization()
analyzer.prepare_classification_data()
analyzer.train_classification_models()
analyzer.visualize_classification_performance()
analyzer.perform_clustering_analysis()
analyzer.visualize_clustering_results()
```
### Пример 2: Работа с CSV файлом
```python
analyzer = AdvancedMLAnalyzer()
analyzer.load_and_prepare_data(
    filepath='your_data.csv',
    target_col='target_column'
)

# Полный анализ
analyzer.exploratory_visualization()
analyzer.prepare_classification_data()
analyzer.train_classification_models()
analyzer.visualize_classification_performance()
analyzer.perform_clustering_analysis()
analyzer.visualize_clustering_results()
```
## 📚 Детальное использование
### Класс `AdvancedMLAnalyzer`

Инициализация
```python
analyzer = AdvancedMLAnalyzer(
    random_state=42  # seed для воспроизводимости
)
```
Основные методы
1. Загрузка данных

```python
df = analyzer.load_and_prepare_data(
    filepath='data.csv',  # путь к файлу
    data=dataframe,       # или DataFrame
    target_col='target'   # целевая переменная
)
```
2. Разведочный анализ

```python
analyzer.exploratory_visualization()
```
Отображает:

+ Распределение целевой переменной

+ Корреляционную матрицу

+ Гистограммы распределений

+ Boxplot для анализа выбросов

+ Парные зависимости

3. Подготовка данных для классификации

```python
X_train, X_test, y_train, y_test = analyzer.prepare_classification_data(
    test_size=0.2,           # размер тестовой выборки
    features_to_drop=None    # признаки для исключения
)
```
4. Обучение моделей классификации

```python
results = analyzer.train_classification_models(
    models_to_use=[          # список моделей для обучения
        'Logistic Regression',
        'Decision Tree',
        'Random Forest',
        'SVM',
        'KNN'
    ]
)
```
5. Визуализация результатов классификации

```python
best_model = analyzer.visualize_classification_performance()
```
6. Создает графики:

+ Сравнение точности моделей

+ Confusion Matrix лучшей модели

+ ROC-кривые (для бинарной классификации)

+ Важность признаков

+ Кривые обучения

+ Сравнение метрик

+ Анализ кластеризации

```python
clustering_results = analyzer.perform_clustering_analysis(
    n_clusters_range=range(2, 8),  # диапазон числа кластеров
    features=None                   # признаки для кластеризации
)
```
7. Визуализация результатов кластеризации

```python
best_algorithm, clusters = analyzer.visualize_clustering_results()
```
Отображает:

+ Сравнение алгоритмов кластеризации

+ PCA проекцию кластеров

+ Распределение по кластерам

+ Характеристики кластеров

+ Тепловую карту средних значений

## 🏗️ Архитектура
### Ключевые компоненты
1. DataProcessor - обработка данных

+ Загрузка из CSV/Excel/JSON

+ Обработка пропущенных значений

+ Масштабирование признаков

+ Кодирование категориальных переменных

2. Classifier - классификация

+ 5 алгоритмов машинного обучения

+ Кросс-валидация

+ Оценка метрик

+ Оптимизация гиперпараметров

3. Clusterer - кластеризация

+ 4 алгоритма кластеризации

+ Определение оптимального числа кластеров

+ Оценка качества кластеризации

4. Visualizer - визуализация

+ EDA графики

+ Метрики производительности

+ Проекции многомерных данных

+ Интерактивные отчеты

## 📊 Примеры использования
### Пример 1: Анализ медицинских данных
```python
# Анализ данных о пациентах
analyzer = AdvancedMLAnalyzer()
analyzer.load_and_prepare_data(
    filepath='medical_data.csv',
    target_col='diagnosis'
)

# Фокус на важных признаках
analyzer.train_classification_models(
    models_to_use=['Random Forest', 'SVM', 'Logistic Regression']
)

# Детальный анализ кластеров пациентов
analyzer.perform_clustering_analysis(
    n_clusters_range=range(2, 6)
)
```
### Пример 2: Сегментация клиентов
```python
# Кластеризация клиентов без целевой переменной
analyzer = AdvancedMLAnalyzer()
analyzer.load_and_prepare_data(
    filepath='customers.csv',
    target_col=None  # Без целевой переменной
)

# Только кластеризация
analyzer.perform_clustering_analysis(
    n_clusters_range=range(3, 8)
)
analyzer.visualize_clustering_results()

# Анализ характеристик кластеров
features_with_clusters = analyzer.features_original.copy()
features_with_clusters['Cluster'] = analyzer.clustering_results['K-Means']['clusters']
cluster_profiles = features_with_clusters.groupby('Cluster').mean()
```
### Пример 3: Сравнение моделей с оптимизацией
```python
# Оптимизация гиперпараметров
analyzer = AdvancedMLAnalyzer()

# Обучение с настройкой
results = analyzer.train_classification_models()

# Сравнение до и после оптимизации
print("Лучшая модель:", max(results, key=lambda x: results[x]['test_accuracy']))
print("Точность лучшей модели:", results[max(results, key=lambda x: results[x]['test_accuracy'])]['test_accuracy'])
```
## 🎨 Визуализации

### Выходные данные включают:
1. Статистические отчеты:

+ Описательная статистика

+ Корреляционные матрицы

+ Распределения признаков

2. Метрики производительности:

+ Accuracy, Precision, Recall, F1-Score

+ ROC-AUC кривые

+ Confusion Matrix

3. Анализ кластеров:

+ Silhouette scores

+ Размеры кластеров

+ Характеристики кластеров

4. Визуализации:

+ 2D/3D проекции

+ Heatmaps

+ Графики сравнения


## 📈 Производительность
### Временные характеристики
|Операция	|1K строк	|10K строк	|100K строк|
|---|---|---|---|
|Загрузка данных	|<1с	|<2с	|<10с|
|Обучение моделей	|5-10с	|30-60с	|5-10мин|
|Кластеризация	|2-5с	|10-20с	|2-5мин|
|Визуализация	|3-5с	|5-10с	|10-20с|
### Оптимизация для больших данных
```python
# Используйте подвыборку для быстрого прототипирования
df_sample = df.sample(frac=0.1, random_state=42)

# Ограничьте количество признаков
analyzer.train_classification_models(
    models_to_use=['Random Forest', 'Logistic Regression']  # Быстрые модели
)

# Используйте PCA для уменьшения размерности
from sklearn.decomposition import PCA
pca = PCA(n_components=10)
X_pca = pca.fit_transform(df.select_dtypes(include=[np.number]))
```
